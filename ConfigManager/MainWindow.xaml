<Window x:Class="ConfigManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cm="clr-namespace:System.ComponentModel;assembly=System"
        xmlns:local="clr-namespace:ConfigManager"
        mc:Ignorable="d"
        Title="Config Manager" Height="450" Width="800" MinHeight="250" MinWidth="200"
        FontSize="14" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <cm:BackgroundWorker x:Key="configBackgroundWorker" WorkerReportsProgress="True" WorkerSupportsCancellation="True"
                             DoWork="BackgroundWorker_DoWork"  ProgressChanged="BackgroundWorker_ProgressChanged"
                             RunWorkerCompleted="BackgroundWorker_RunWorkerCompleted">
        </cm:BackgroundWorker>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label x:Name="pluginLabel" Content="Plugin" Grid.Row="0" Grid.Column="0" Margin="10,10,0,0" HorizontalAlignment="Left"/>
        <ComboBox x:Name="pluginComboBox" Grid.Row="1" Grid.Column="0" Margin="10,0,0,10" HorizontalAlignment="Stretch" Height="25">
            <ComboBoxItem Content="All" IsSelected="True" />
            <ComboBoxItem Content="Adjuster" />
            <ComboBoxItem Content="AuditArchive" />
            <ComboBoxItem Content="AutoDialer2.0" />
            <ComboBoxItem Content="BasicDBMaint" />
            <ComboBoxItem Content="DBI_DailyActivityReport" />
            <ComboBoxItem Content="EDI_Archive" />
            <ComboBoxItem Content="Notes" />
            <ComboBoxItem Content="ProfitPal" />
            <ComboBoxItem Content="Reporter" />
            <ComboBoxItem Content="Uploader" />
        </ComboBox>

        <Label x:Name="dateLabel" Content="Date Range" Grid.Row="0" Grid.Column="1" Margin="10,10,0,0" HorizontalAlignment="Left"/>
        <ComboBox x:Name="dateComboBox" Grid.Row="1" Grid.Column="1" Margin="10,0,0,10" HorizontalAlignment="Stretch" Height="25">
            <ComboBoxItem Content="All Time" IsSelected="True" />
            <ComboBoxItem Content="Today" />
            <ComboBoxItem Content="Past Week" />
            <ComboBoxItem Content="Past Month" />
            <ComboBoxItem Content="Past Year" />
        </ComboBox>

        <Button x:Name="submitButton" Content="Go" Grid.Row="1" Grid.Column="2" Margin="10,0,0,10" HorizontalAlignment="Left" Width="75" Height="25" Click="SubmitButton_Click"/>
        
        <Button x:Name="cancelButton" Content="Cancel" IsEnabled="False" Grid.Row="1" Grid.Column="3" Margin="10,0,10,10" HorizontalAlignment="Left" Width="75" Height="25" Click="CancelButton_Click"/>

        <Button x:Name="clearSelectionButton" Content="Clear Selection" IsEnabled="False" Grid.Row="2" Grid.Column="0" Margin="10,20,0,0" HorizontalAlignment="Left" Width="120" Height="25" Click="ClearSelectionButton_Click" />

        <Label x:Name="selectedLabel" Grid.Row="2" Grid.Column="1" Margin="0,20,10,0" HorizontalAlignment="Left"/>
        <Label x:Name="countLabel" Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" Margin="0,20,10,0" HorizontalAlignment="Left"/>

        <DataGrid x:Name="configDataGrid" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="4" Margin="10" IsReadOnly="True" 
                  SelectionMode="Extended" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                  SelectionChanged="ConfigDataGrid_SelectionChanged">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="File has changes in SVN">
                            <Setter Property="Background" Value="#FFC8FFC8"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="File has changes in VMServices">
                            <Setter Property="Background" Value="#FFFFC8C8"></Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="File has has not been deployed to VMServices">
                            <Setter Property="Background" Value="#FFC8C8FF"></Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <ProgressBar x:Name="configProgressBar" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="4" Margin="10,0,10,20" Height="18"/>

        <Button x:Name="grabButton" Content="GRAB" IsEnabled="False" Grid.Row="5" Grid.Column="0" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Height="25"/>
        <Button x:Name="deployButton" Content="DEPLOY" IsEnabled="False" Grid.Row="5" Grid.Column="3" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Top" Width="75" Height="25"/>

    </Grid>
</Window>
